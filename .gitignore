# Claude local settings (machine-specific, never commit)
.claude/settings.local.json

# Local environment overrides — never commit secrets
.env

# IDE files
.idea/

# Claude agent worktrees (temporary, machine-specific)
.claude/worktrees/

# Agent Teams Lite registry artifacts
.atl/

# AI tool local artifacts (machine-specific, never commit)
.agents/
.claude/skills/
.kiro/
.trae/
.pi/
context.md
sdd-verify-*.md

# Local memory snapshots — never commit
.engram/
