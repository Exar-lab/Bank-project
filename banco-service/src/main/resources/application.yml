spring:
  application:
    name: banco-service
  datasource:
    url: ${DB_URL}
    password: ${DB_PASSWORD}
    username: ${DB_USERNAME}
  jpa:
    hibernate:
      ddl-auto: validate

# =============================================
# Kafka
# =============================================
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer
      acks: all
      retries: 3
      properties:
        max.in.flight.requests.per.connection: 1
        enable.idempotence: true
    consumer:
      group-id: banco-service-group
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      enable-auto-commit: false

# =============================================
# Flyway
# =============================================
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: false
    baseline-version: 0

# =============================================
# Scheduling
# =============================================
  task:
    scheduling:
      enabled: true
  thymeleaf:
    cache: true

# =============================================
# Dotenv (Opción C: .env compartido con Docker Compose)
# =============================================
springdotenv:
  directory: ${SPRINGDOTENV_DIRECTORY:..}

# =============================================
# Jasypt - Encriptación
# =============================================
jasypt:
  encryptor:
    password: ${JASYPT_ENCRYPTOR_PASSWORD}
    algorithm: PBEWithMD5AndDES
    iv-generator-classname: org.jasypt.iv.NoIvGenerator

security:
  jwt:
    secret-key: ${JWT_SECRET_KEY}
    issuer: ${ISSUER_GENERATOR}
    access-token:
      expiration-minutes: 15  # Access token corto
    refresh-token:
      expiration-days: 30     # Refresh token largo

# =============================================
# Fraud Detection
# =============================================
fraud:
  detection:
    risk-profile-enabled: false
    suspicious-threshold: 10000000
    blocked-threshold: 50000000
    risk-profile-query-timeout-ms: 25
    risk-profile-fallback-policy: FAIL_OPEN_LEGACY_THRESHOLD
    risk-profile-async:
      consumer-name: risk-profile-consumer
      medium-threshold: 10000000
      high-threshold: 50000000
      restricted-threshold: 100000000
      low-score: 25.00
      medium-score: 60.00
      high-score: 85.00
      restricted-score: 95.00
      rule-set-version: v1

banco:
  mail:
    enabled: ${MAIL_ENABLED:true}
    host: ${MAIL_HOST}
    port: ${MAIL_PORT:587}
    username: ${MAIL_USERNAME}
    password: ${MAIL_PASSWORD}
    from: ${MAIL_FROM:no-reply@banco.co}
    relay:
      batch-size: ${MAIL_RELAY_BATCH_SIZE:50}
      poll-delay-ms: ${MAIL_RELAY_POLL_DELAY_MS:10000}
      max-attempts: ${MAIL_RELAY_MAX_ATTEMPTS:5}
    executor:
      core-pool-size: ${MAIL_EXECUTOR_CORE_POOL_SIZE:2}
      max-pool-size: ${MAIL_EXECUTOR_MAX_POOL_SIZE:8}
      queue-capacity: ${MAIL_EXECUTOR_QUEUE_CAPACITY:500}
